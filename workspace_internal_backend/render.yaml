services:
  - type: web
    name: hrms-backend
    runtime: node
    plan: free
    buildCommand: npm install && node insert-test-user.js
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
