export enum Gender {
    Male = 'male',
    Female = 'female',
}

export enum LeaveDuration {
    Single = 'single',
    Multiple = 'multiple',
    HalfDay = 'half',
}
export enum ProviderStatus {
    pending = "pending",
    approved = "approved",
    rejected = "rejected",
    cancelled = "cancelled",
}
export enum ProviderStatusColor {
    pending = "#d27b3a",
    approved = "#209563",
    rejected = "#f44336",
    cancelled = "#f44337"
}

export enum ResignationStatus {
    pending = "pending",
    accepted = "accepted",
    rejected = "rejected",
}
export enum ResignationStatusColor {
    pending = "#d27b3a",
    accepted = "#209563",
    rejected = "#f44336",
}